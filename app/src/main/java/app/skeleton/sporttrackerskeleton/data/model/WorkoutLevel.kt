package app.skeleton.sporttrackerskeleton.data.model

import androidx.annotation.StringRes

enum class WorkoutLevel(@field:StringRes val titleRes: Int) {
    // for example
    // BEGINNER(R.string.beginner_level)
    // INTERMEDIATE(R.string.intermediate_level)
    // ADVANCED(R.string.advanced_level)
}